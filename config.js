const config = {
    db: {
        host: 'localhost',
        user: 'root',
        password: 'password',
        database: 'vulnerable_db'
    },
    server: {
        port: 3000
    }
};

module.exports = config;